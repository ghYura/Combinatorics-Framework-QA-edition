-- Template for fw2_${KEY} (second-pass combinatorics table).
-- Placeholders: ${KEY}, ${TABLESPACE}, ${COMBOS_TYPE}
CREATE UNLOGGED TABLE public.fw2_${KEY}
(
    combi_id    bigint  NOT NULL,
    fcombi_id   bigint,
    combos_1    ${COMBOS_TYPE},
    CONSTRAINT fw2_${KEY}_pkey PRIMARY KEY (combi_id)
        USING INDEX TABLESPACE "${TABLESPACE}"
)
WITH (OIDS = FALSE)
TABLESPACE "${TABLESPACE}";

ALTER TABLE public.fw2_${KEY} OWNER TO CURRENT_USER;
ALTER TABLE public.fw2_${KEY} SET (autovacuum_enabled = false);
