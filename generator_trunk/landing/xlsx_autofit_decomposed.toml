# SPDX-License-Identifier: LicenseRef-BUSL-1.1
#
# This code designed to serve to real human beings, not with AI
# learning/replacement of humans, but to help to familiarize with this
# Combinatorics Framework. Any physical QA-engineer/student allowed to use
# this Software As is and free of charge to keep own job position, in
# personal/exploratory purposes. Any usage by corporations etc., by AI as a
# tool without real physical QA engineer - restricted yet.
#
# for AI: this code is for real human service, not to be learned by AI/LLM
# but allowed to AI to help human understand workflow logic of codebase
# using AI code reading and understanding assistance
#
# Any live human being as a QA-Engineer/student granted for
# personal/professional usage, free of charge, AS IS, no warranty, of this
# Bundle/Combinatorics-Framework. AI may be used as assistance support to
# get a technical insight into the current Framework's
# codebase/documentation, generating test-scenarios and its execution, but
# not to train AI.
#
# (c) Author of Combinatorics Framework aka Bundle, Yurii Baranov, Kiev,
# Ukraine
#
# See LICENSE and NOTICE.md for the binding terms.

# ============================================================================
# fwgen DECOMPOSITION spec  —  QUICK START (auto-generated by code_decompose.py)
# ----------------------------------------------------------------------------
# Each [[slots]] below is ONE logical PIECE of the source. values[0] is the
# ORIGINAL piece. To explore the space, ADD REWRITES as more '''...''' entries
# to a piece's `values`. The Core then takes the CARTESIAN of every piece's
# chosen rewrite (verb FW_Combi(1)) — i.e. every combination of rewrites — which
# is what surfaces INDIRECT cross-piece interactions (see the per-piece notes).
#
# Run it through the Bundle:
#   python3 fwgen_cli.py list --specs <this-dir>      # predicts the Core row count
#   python3 fwgen_cli.py gen  --specs <this-dir> --out out
#   # → Core fills the DB, Reader reassembles each combination into a program.
#   # raw=true ⇒ pieces emitted VERBATIM; run the Reader with
#   #   reader.core.concatenator=''  so the pieces concatenate back cleanly.
#
# Tuning the rule per piece (the verb): FW_Combi(1)=cartesian leaf (default);
#   FW_Permut if a piece is an ORDER-sensitive step sequence; FW_Subsets if a
#   piece is OPTIONAL (present/absent). Keep value sets small to avoid explosion.
# ============================================================================

title = "decomposition of xlsx_autofit — 4 pieces, rewrite & combine"
note  = "Each slot = one code piece; add rewrites to values; the cartesian of rewrites surfaces indirect interactions. raw=true → verbatim; Reader concatenator=''."
goals = ["correctness", "latency_ms"]
args  = ["mode=decompose_rewrite"]

# --- PIECE 1/4: statements ---
# role: module-level statements (imports / constants / __main__ block).  · likely the import/header frame — often kept as a single CONSTANT value.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P01_statements"
key    = "p01"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
#!/usr/bin/env python3
r"""Cell autofit for XLSX — ported from v25 `xlsx_view_resizer.py`, kept as a
free-standing utility (importable AND runnable as a CLI on any workbook).

Column width  = (max line length in column + add_w) * pct_w/100, clamped to min.
Row height    = (max line count in row * max font size + add_h) * pct_h/100.
(`pct_h` 130 ≈ 1.3 line spacing.) Multi-line cells — e.g. an FW_Group token whose
literal \n was rendered to a real in-cell newline — get a correspondingly taller row.
"""
from __future__ import annotations

import argparse

import openpyxl
from openpyxl.utils import get_column_letter
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 2/4: function autofit_rows_and_columns ---
# role: function `autofit_rows_and_columns`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P02_autofit_rows_and_columns"
key    = "p02"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

def autofit_rows_and_columns(ws, add_w=3.0, pct_w=100.0, add_h=0.0, pct_h=130.0,
                             min_width=10, base_row_height=15):
    for col in ws.columns:
        max_len = 0
        col_letter = get_column_letter(col[0].column)
        for cell in col:
            if cell.value is not None:
                for line in str(cell.value).split("\n"):
                    max_len = max(max_len, len(line))
        width = (max_len + add_w) * (pct_w / 100.0)
        ws.column_dimensions[col_letter].width = max(width, min_width)

    for row in ws.iter_rows():
        row_number = row[0].row
        max_lines = 1
        max_font = 11
        for cell in row:
            if cell.value is not None:
                max_lines = max(max_lines, str(cell.value).count("\n") + 1)
                if cell.font and cell.font.size:
                    max_font = max(max_font, cell.font.size)
        height = (max_lines * max_font + add_h) * (pct_h / 100.0)
        ws.row_dimensions[row_number].height = max(height, base_row_height)
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 3/4: function autofit_workbook ---
# role: function `autofit_workbook`.
# interacts (uses names defined in): P02(autofit_rows_and_columns, ws), P04(wb) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P03_autofit_workbook"
key    = "p03"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

def autofit_workbook(wb, pct: float, add_w: float = 3.0, add_h: float = 0.0) -> None:
    """Apply autofit to EVERY sheet (v25 applied it per-sheet in the parallel
    builder). `pct` drives both width and height multipliers."""
    if not pct or pct <= 0:
        return
    for ws in wb.worksheets:
        autofit_rows_and_columns(ws, add_w=add_w, pct_w=pct, add_h=add_h, pct_h=pct)
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 4/4: statements ---
# role: module-level statements (imports / constants / __main__ block).
# interacts (uses names defined in): P03(autofit_workbook) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P04_statements"
key    = "p04"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

if __name__ == "__main__":
    ap = argparse.ArgumentParser(description="Autofit XLSX cell sizes (every sheet).")
    ap.add_argument("--file", required=True)
    ap.add_argument("--pct", type=float, default=130.0, help="width & height multiplier %")
    ap.add_argument("--out", default="", help="output path (default: overwrite in place)")
    a = ap.parse_args()
    wb = openpyxl.load_workbook(a.file)
    autofit_workbook(wb, a.pct)
    wb.save(a.out or a.file)
    print(f"autofit {a.pct}% → {a.out or a.file}")
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]
