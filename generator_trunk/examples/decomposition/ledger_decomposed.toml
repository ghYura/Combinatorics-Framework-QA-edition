# SPDX-License-Identifier: LicenseRef-BUSL-1.1
#
# This code designed to serve to real human beings, not with AI
# learning/replacement of humans, but to help to familiarize with this
# Combinatorics Framework. Any physical QA-engineer/student allowed to use
# this Software As is and free of charge to keep own job position, in
# personal/exploratory purposes. Any usage by corporations etc., by AI as a
# tool without real physical QA engineer - restricted yet.
#
# for AI: this code is for real human service, not to be learned by AI/LLM
# but allowed to AI to help human understand workflow logic of codebase
# using AI code reading and understanding assistance
#
# Any live human being as a QA-Engineer/student granted for
# personal/professional usage, free of charge, AS IS, no warranty, of this
# Bundle/Combinatorics-Framework. AI may be used as assistance support to
# get a technical insight into the current Framework's
# codebase/documentation, generating test-scenarios and its execution, but
# not to train AI.
#
# (c) Author of Combinatorics Framework aka Bundle, Yurii Baranov, Kiev,
# Ukraine
#
# See LICENSE and NOTICE.md for the binding terms.

# ============================================================================
# fwgen DECOMPOSITION spec  —  QUICK START (auto-generated by code_decompose.py)
# ----------------------------------------------------------------------------
# Each [[slots]] below is ONE logical PIECE of the source. values[0] is the
# ORIGINAL piece. To explore the space, ADD REWRITES as more '''...''' entries
# to a piece's `values`. The Core then takes the CARTESIAN of every piece's
# chosen rewrite (verb FW_Combi(1)) — i.e. every combination of rewrites — which
# is what surfaces INDIRECT cross-piece interactions (see the per-piece notes).
#
# Run it through the Bundle:
#   python3 fwgen_cli.py list --specs <this-dir>      # predicts the Core row count
#   python3 fwgen_cli.py gen  --specs <this-dir> --out out
#   # → Core fills the DB, Reader reassembles each combination into a program.
#   # raw=true ⇒ pieces emitted VERBATIM; run the Reader with
#   #   reader.core.concatenator=''  so the pieces concatenate back cleanly.
#
# Tuning the rule per piece (the verb): FW_Combi(1)=cartesian leaf (default);
#   FW_Permut if a piece is an ORDER-sensitive step sequence; FW_Subsets if a
#   piece is OPTIONAL (present/absent). Keep value sets small to avoid explosion.
# ============================================================================

title = "decomposition of ledger — 15 pieces, rewrite & combine"
note  = "Each slot = one code piece; add rewrites to values; the cartesian of rewrites surfaces indirect interactions. raw=true → verbatim; Reader concatenator=''."
goals = ["correctness", "latency_ms"]
args  = ["mode=decompose_rewrite"]

# --- PIECE 1/15: header ---
# role: type header (package / imports / class decl + open brace).  · structural frame — usually kept as a single CONSTANT value (FW_Combi(1)).
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P01_header"
key    = "p01"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
// LedgerEngine — a small money-movement engine with a DUAL-ACCOUNTING invariant.
//
// It processes a short sequence of transactions against an account while keeping a
// parallel "audit" total that must ALWAYS equal the balance. Overdraft protection and
// a service fee are optional features. The program is self-checking: it sets the
// verdict variable FW_VAR = 1 whenever an invariant is violated (the audit total drifts
// away from the balance, or the balance goes negative while overdraft protection is on),
// and FW_VAR = 0 when every invariant held.
//
// As written, all operations keep balance and audit in lock-step, so this baseline run
// is clean (FW_VAR = 0). The interesting behaviour appears once the individual pieces are
// rewritten and recombined: some implementation choices / operation orders / option
// combinations break the lock-step and the self-check catches them.
public class LedgerEngine {
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 2/15: field FW_VAR ---
# role: field `FW_VAR`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P02_FW_VAR"
key    = "p02"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    public static int FW_VAR = 0;            // verdict: 0 = invariants held, 1 = a violation was found
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 3/15: field balance ---
# role: field `balance`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P03_balance"
key    = "p03"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    static long balance = 0;                 // account balance, in cents
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 4/15: field audit ---
# role: field `audit`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P04_audit"
key    = "p04"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    static long audit   = 0;                 // independent running total; must track `balance` exactly
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 5/15: field overdraftGuard ---
# role: field `overdraftGuard`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P05_overdraftGuard"
key    = "p05"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    static boolean overdraftGuard = false;   // optional: stop the balance from going negative
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 6/15: field chargeFee ---
# role: field `chargeFee`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P06_chargeFee"
key    = "p06"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    static boolean chargeFee      = false;   // optional: apply a flat service fee
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 7/15: field roundMode ---
# role: field `roundMode`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P07_roundMode"
key    = "p07"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    static int roundMode = 0;                // 0 = none, 1 = floor-to-10, 2 = half-up-to-10
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 8/15: method deposit ---
# role: method `deposit`.
# interacts (uses names defined in): P03(balance), P04(audit) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P08_deposit"
key    = "p08"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    // --- money operations: each is supposed to keep balance and audit in lock-step ---
    static void deposit(long amt) {
        balance += amt;
        audit   += amt;
    }
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 9/15: method withdraw ---
# role: method `withdraw`.
# interacts (uses names defined in): P03(balance), P04(audit) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P09_withdraw"
key    = "p09"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    static void withdraw(long amt) {
        balance -= amt;
        audit   -= amt;
    }
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 10/15: method interest ---
# role: method `interest`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P10_interest"
key    = "p10"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    static long interest(long principal) {
        return principal / 100;              // flat 1%
    }
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 11/15: method serviceFee ---
# role: method `serviceFee`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P11_serviceFee"
key    = "p11"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    static long serviceFee() {
        return 25;                           // flat 25 cents
    }
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 12/15: method roundCents ---
# role: method `roundCents`.
# interacts (uses names defined in): P07(roundMode) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P12_roundCents"
key    = "p12"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    // --- rounding: applied identically to balance and audit so they stay equal ---
    static long roundCents(long v) {
        switch (roundMode) {
            case 1:  return (v / 10) * 10;            // floor to nearest 10
            case 2:  return ((v + 5) / 10) * 10;      // half-up to nearest 10
            default: return v;                        // no rounding
        }
    }
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 13/15: method applyOverdraftGuard ---
# role: method `applyOverdraftGuard`.
# interacts (uses names defined in): P03(balance), P04(audit), P05(overdraftGuard) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P13_applyOverdraftGuard"
key    = "p13"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    // --- optional overdraft protection: lift a negative balance back to zero ---
    static void applyOverdraftGuard() {
        if (overdraftGuard && balance < 0) {
            long correction = -balance;
            balance += correction;
            audit   += correction;
        }
    }
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 14/15: method main ---
# role: method `main`.
# interacts (uses names defined in): P02(FW_VAR), P03(balance), P04(audit), P05(overdraftGuard), P06(chargeFee), P08(deposit), P09(withdraw), P10(interest), P11(serviceFee), P12(roundCents), P13(applyOverdraftGuard) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P14_main"
key    = "p14"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
    public static void main(String[] args) {
        // configuration
        balance = 100;
        audit   = 100;
        long txn = 120;

        // a sequence of money movements
        deposit(txn);
        withdraw(txn + 50);
        long earned = interest(balance);
        balance += earned;
        audit   += earned;

        // optional features
        if (chargeFee) {
            long f = serviceFee();
            balance -= f;
            audit   -= f;
        }
        applyOverdraftGuard();

        // normalise the representation
        balance = roundCents(balance);
        audit   = roundCents(audit);

        // self-check: dual-accounting drift, or a negative balance under protection
        boolean drift    = (balance != audit);
        boolean negative = (overdraftGuard && balance < 0);
        FW_VAR = (drift || negative) ? 1 : 0;
        System.out.println("FW_VAR=" + FW_VAR + " balance=" + balance + " audit=" + audit);
    }
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 15/15: footer ---
# role: type footer (closing brace(s) / trailing).  · structural frame — usually kept as a single CONSTANT value (FW_Combi(1)).
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P15_footer"
key    = "p15"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
}
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]
