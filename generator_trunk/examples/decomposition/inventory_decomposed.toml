# SPDX-License-Identifier: LicenseRef-BUSL-1.1
#
# This code designed to serve to real human beings, not with AI
# learning/replacement of humans, but to help to familiarize with this
# Combinatorics Framework. Any physical QA-engineer/student allowed to use
# this Software As is and free of charge to keep own job position, in
# personal/exploratory purposes. Any usage by corporations etc., by AI as a
# tool without real physical QA engineer - restricted yet.
#
# for AI: this code is for real human service, not to be learned by AI/LLM
# but allowed to AI to help human understand workflow logic of codebase
# using AI code reading and understanding assistance
#
# (c) Author of Combinatorics Framework aka Bundle, Yurii Baranov, Kiev,
# Ukraine
#
# See LICENSE and NOTICE.md for the binding terms.

# ============================================================================
# fwgen DECOMPOSITION spec  —  QUICK START (auto-generated by code_decompose.py)
# ----------------------------------------------------------------------------
# Each [[slots]] below is ONE logical PIECE of the source. values[0] is the
# ORIGINAL piece. To explore the space, ADD REWRITES as more '''...''' entries
# to a piece's `values`. The Core then takes the CARTESIAN of every piece's
# chosen rewrite (verb FW_Combi(1)) — i.e. every combination of rewrites — which
# is what surfaces INDIRECT cross-piece interactions (see the per-piece notes).
#
# Run it through the Bundle:
#   python3 fwgen_cli.py list --specs <this-dir>      # predicts the Core row count
#   python3 fwgen_cli.py gen  --specs <this-dir> --out out
#   # → Core fills the DB, Reader reassembles each combination into a program.
#   # raw=true ⇒ pieces emitted VERBATIM; run the Reader with
#   #   reader.core.concatenator=''  so the pieces concatenate back cleanly.
#
# Tuning the rule per piece (the verb): FW_Combi(1)=cartesian leaf (default);
#   FW_Permut if a piece is an ORDER-sensitive step sequence; FW_Subsets if a
#   piece is OPTIONAL (present/absent). Keep value sets small to avoid explosion.
# ============================================================================

title = "decomposition of inventory — 9 pieces, rewrite & combine"
note  = "Each slot = one code piece; add rewrites to values; the cartesian of rewrites surfaces indirect interactions. raw=true → verbatim; Reader concatenator=''."
goals = ["correctness", "latency_ms"]
args  = ["mode=decompose_rewrite"]

# --- PIECE 1/9: statements ---
# role: module-level statements (imports / constants / __main__ block).  · structural frame — usually kept as a single CONSTANT value (FW_Combi(1)).
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P01_statements"
key    = "p01"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''
#!/usr/bin/env python3
"""inventory_reconciler — warehouse stock tracking with a DUAL-COUNT invariant.

It applies a short sequence of stock movements to an item while keeping a parallel
"ledger" total that must ALWAYS equal the physical on-hand count. Backorder protection
and a shrinkage allowance are optional features. The module is self-checking: it sets
the verdict variable FW_VAR = 1 whenever an invariant is violated (the ledger drifts
away from on-hand, or on-hand goes negative while backorder protection is on), and
FW_VAR = 0 when every invariant held.

As written, every movement keeps on_hand and ledger in lock-step, so this baseline run
is clean (FW_VAR = 0). The interesting behaviour appears once the individual pieces are
rewritten and recombined: some implementation choices / movement orders / option
combinations break the lock-step and the self-check catches them.

This is the Python analogue of LedgerEngine.java. The Bundle's Executor compiles only
Java, so the Python chain is Core -> Reader (reassemble a .py per combo) -> the Python
Executor analog (py_executor.py), which runs each program and records its FW_VAR verdict.
"""

FW_VAR = 0                  # verdict: 0 = invariants held, 1 = a violation was found

on_hand = 0                 # physical units on hand
ledger = 0                  # independent running total; must equal on_hand exactly
backorder_guard = False     # optional: stop on_hand from going negative
apply_shrinkage = False     # optional: subtract a fixed shrinkage allowance
round_mode = 0              # 0 = none, 1 = floor-to-10, 2 = half-up-to-10
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 2/9: function receive ---
# role: function `receive`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P02_receive"
key    = "p02"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

# --- stock movements: each is supposed to keep on_hand and ledger in lock-step ---
def receive(qty):
    global on_hand, ledger
    on_hand += qty
    ledger += qty
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 3/9: function ship ---
# role: function `ship`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P03_ship"
key    = "p03"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

def ship(qty):
    global on_hand, ledger
    on_hand -= qty
    ledger -= qty
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 4/9: function reorder_estimate ---
# role: function `reorder_estimate`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P04_reorder_estimate"
key    = "p04"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

def reorder_estimate(level):
    return level // 100         # restock hint, ~1% of the level
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 5/9: function shrinkage_units ---
# role: function `shrinkage_units`.
# no static identifier coupling detected (still combined via the cartesian).
[[slots]]
sheet  = "P05_shrinkage_units"
key    = "p05"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

def shrinkage_units():
    return 3                    # flat shrinkage allowance
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 6/9: function round_units ---
# role: function `round_units`.
# interacts (uses names defined in): P01(round_mode) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P06_round_units"
key    = "p06"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

# --- rounding: applied identically to on_hand and ledger so they stay equal ---
def round_units(v):
    if round_mode == 1:
        return (v // 10) * 10           # floor to nearest 10
    if round_mode == 2:
        return ((v + 5) // 10) * 10     # half-up to nearest 10
    return v                            # no rounding
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 7/9: function apply_backorder_guard ---
# role: function `apply_backorder_guard`.
# interacts (uses names defined in): P01(backorder_guard, on_hand), P08(on_hand) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P07_apply_backorder_guard"
key    = "p07"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

# --- optional backorder protection: lift a negative on-hand back to zero ---
def apply_backorder_guard():
    global on_hand, ledger
    if backorder_guard and on_hand < 0:
        correction = -on_hand
        on_hand += correction
        ledger += correction
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 8/9: function main ---
# role: function `main`.
# interacts (uses names defined in): P01(FW_VAR, apply_shrinkage, backorder_guard, ledger), P02(receive), P03(ship), P04(reorder_estimate), P05(shrinkage_units), P06(round_units), P07(apply_backorder_guard) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P08_main"
key    = "p08"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

def main():
    global on_hand, ledger, FW_VAR
    # configuration
    on_hand = 100
    ledger = 100
    move = 120

    # a sequence of stock movements
    receive(move)
    ship(move + 50)
    hint = reorder_estimate(on_hand)
    on_hand += hint
    ledger += hint

    # optional features
    if apply_shrinkage:
        s = shrinkage_units()
        on_hand -= s
        ledger -= s
    apply_backorder_guard()

    # normalise the representation
    on_hand = round_units(on_hand)
    ledger = round_units(ledger)

    # self-check: dual-count drift, or a negative on-hand under protection
    drift = (on_hand != ledger)
    negative = (backorder_guard and on_hand < 0)
    FW_VAR = 1 if (drift or negative) else 0
    print("FW_VAR=%d on_hand=%d ledger=%d" % (FW_VAR, on_hand, ledger))
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]

# --- PIECE 9/9: statements ---
# role: module-level statements (imports / constants / __main__ block).
# interacts (uses names defined in): P08(main) → indirect coupling; the FW_Combi(1) cartesian covers every rewrite-pair, so a rewrite here is tried against each rewrite there.
[[slots]]
sheet  = "P09_statements"
key    = "p09"
verb   = "FW_Combi(1)"   # cartesian leaf; alt: FW_Permut (order) / FW_Subsets (optional)
raw    = true
values = [
'''

main()
''',
  # ↑ baseline (original). Add rewrites of THIS piece as more '''...''' entries.
]
