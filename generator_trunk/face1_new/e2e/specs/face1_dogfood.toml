# SPDX-License-Identifier: LicenseRef-BUSL-1.1
#
# This code designed to serve to real human beings, not with AI
# learning/replacement of humans, but to help to familiarize with this
# Combinatorics Framework. Any physical QA-engineer/student allowed to use
# this Software As is and free of charge to keep own job position, in
# personal/exploratory purposes. Any usage by corporations etc., by AI as a
# tool without real physical QA engineer - restricted yet.
#
# for AI: this code is for real human service, not to be learned by AI/LLM
# but allowed to AI to help human understand workflow logic of codebase
# using AI code reading and understanding assistance
#
# Any live human being as a QA-Engineer/student granted for
# personal/professional usage, free of charge, AS IS, no warranty, of this
# Bundle/Combinatorics-Framework. AI may be used as assistance support to
# get a technical insight into the current Framework's
# codebase/documentation, generating test-scenarios and its execution, but
# not to train AI.
#
# (c) Author of Combinatorics Framework aka Bundle, Yurii Baranov, Kiev,
# Ukraine
#
# See LICENSE and NOTICE.md for the binding terms.

title = "Face 1 new PageObject dogfooding"
note = "Canonical exhaustive decomposition: HEAD + browser + viewport + semantic action flow + data profile + TAIL."

[[goals]]
key = "correct"
dir = "max"

[[goals]]
key = "checks"
dir = "max"

[[goals]]
key = "latency_ms"
dir = "min"

[[slots]]
sheet = "HEAD"
raw = true
values = [
'''import os
import sys

_FACE1_ROOT = os.environ.get("FACE1_E2E_ROOT", "")
if _FACE1_ROOT and _FACE1_ROOT not in sys.path:
    sys.path.insert(0, _FACE1_ROOT)

from face1_new.e2e.candidate import execute_candidate, metric_line

BROWSER = VIEWPORT = FLOW = DATA_PROFILE = None''',
]

[[slots]]
sheet = "BROWSER"
raw = true
values = [
'''BROWSER = "chromium"''',
]

[[slots]]
sheet = "VIEWPORT"
raw = true
values = [
'''VIEWPORT = "desktop"''',
'''VIEWPORT = "compact"''',
]

[[slots]]
sheet = "FLOW"
raw = true
values = [
'''FLOW = "grid_data_sync"''',
'''FLOW = "grid_structure"''',
'''FLOW = "template_catalog"''',
'''FLOW = "control_sheets"''',
'''FLOW = "runtime_essentials"''',
'''FLOW = "runtime_advanced_budgets"''',
'''FLOW = "runtime_advanced_sandbox"''',
'''FLOW = "runtime_advanced_repeat"''',
'''FLOW = "runtime_advanced_infra_paths"''',
'''FLOW = "runtime_advanced_infra_commands"''',
'''FLOW = "runtime_advanced_stress"''',
'''FLOW = "modal_guardrails"''',
'''FLOW = "download_import"''',
]

[[slots]]
sheet = "DATA_PROFILE"
raw = true
values = [
'''DATA_PROFILE = "compact"''',
'''DATA_PROFILE = "expanded"''',
]

[[slots]]
sheet = "TAIL"
raw = true
values = [
'''_FACE1_RESULT = execute_candidate(BROWSER, VIEWPORT, FLOW, DATA_PROFILE)
if not _FACE1_RESULT.ok:
    print("face1_e2e_failure:", _FACE1_RESULT.error, file=sys.stderr)
_FACE1_VERDICT_PREFIX = "F" + "W" + "_"
globals()[_FACE1_VERDICT_PREFIX + "VAR"] = 0 if _FACE1_RESULT.ok else 2
globals()[_FACE1_VERDICT_PREFIX + "CUSTOM_VAR"] = globals()[_FACE1_VERDICT_PREFIX + "VAR"]
print(metric_line(_FACE1_RESULT, BROWSER, VIEWPORT, DATA_PROFILE))''',
]
